name: CD Pipeline

# UNCOMMENT THE SECTION BELOW TO ENABLE THIS WORKFLOW
# on:
#   push:
#     branches: [ "main", "master" ] # Only deploy on main/master

jobs:
  deploy:
    name: Build and Deploy (Placeholder)
    runs-on: ubuntu-latest
    
    # Needs ensures deployment only happens if CI passes
    # needs: [backend-test, frontend-test] # If using a single file
    
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      # Placeholder: Build Docker images
      # - name: Build and Push Docker Images
      #   run: |
      #     docker-compose build
      #     docker push your-registry/backend
      #     docker push your-registry/frontend

      # Placeholder: Deploy to Server via SSH
      # - name: Deploy to Server
      #   uses: appleboy/ssh-action@master
      #   with:
      #     host: ${{ secrets.SERVER_HOST }}
      #     username: ${{ secrets.SERVER_USER }}
      #     key: ${{ secrets.SERVER_SSH_KEY }}
      #     script: |
      #       cd /path/to/project
      #       git pull origin main
      #       docker-compose pull
      #       docker-compose up -d --remove-orphans
